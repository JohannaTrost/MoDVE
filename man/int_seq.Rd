% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{int_seq}
\alias{int_seq}
\title{Custom sequence function for integers and floats with zero fractional part}
\usage{
int_seq(from, to, by = 1L)
}
\arguments{
\item{from}{The starting value of the sequence (integer or float with zero fractional part).}

\item{to}{The ending value of the sequence (integer or float with zero fractional part).}

\item{by}{The increment between values in the sequence (non-zero integer or float with zero
fractional part, default=1).}
}
\value{
\itemize{
\item A vector of integers generated using \code{seq.int} if the input is valid.
\item An empty vector if \code{by} is positive and \code{from} is greater than \code{to},
or \code{by} is negative and \code{from} is less than \code{to}.
\item An error message if either \code{from}, \code{to} or \code{by} has a non-zero fractional part,
or if \code{by} is zero.
}
}
\description{
This function generates a sequence of integers similar to \code{seq.int}, but
with additional checks and behaviors:
}
\details{
\itemize{
\item It avoids using the colon operator (':').
\item It ensures that \code{from}, \code{to} and \code{by} are integers or floats with zero fractional part.
\item It ensures that \code{by} is non-zero.
}
}
\examples{
# Valid usage:
int_seq(from = 2, to = 5, by = 1)     # [1] 2 3 4 5
int_seq(from = 5.0, to = 3, by = -1)  # [1] 5 4 3
int_seq(from = 4, to = 4, by = -1)    # [1] 4
int_seq(from = 4, to = 5, by = -1)    # integer(0)
int_seq(from = 2, to = 1, by = 2)     # integer(0)

# Invalid usage:
int_seq(from = 2.5, to = 5, by = 1)   # Error: Fractional part in 'from', 'to' or 'by'
int_seq(from = 2, to = 5, by = 0)     # Error: 'by' must be non-zero
}
